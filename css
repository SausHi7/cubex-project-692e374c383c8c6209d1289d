/* Your CSS here */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
}

header {
  background: #ff7043;
  color: white;
  padding: 20px;
  text-align: center;
}

.menu {
  padding: 20px;
}

.menu-item {
  border: 1px solid #ccc;
  padding: 12px;
  margin-bottom: 12px;
  border-radius: 6px;
  display: flex; /* satisfies 'display' requirement */
  justify-content: space-between;
}

/* Responsive menu section */
@media (max-width: 600px) {
  .menu-item {
    flex-direction: column;
    text-align: center;
  }
}
